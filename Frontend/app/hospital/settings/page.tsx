"use client";

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Bell, Building2, CalendarDays, ChevronDown, LayoutDashboard, LogOut, Menu, Save, Settings, ShieldCheck, Users, X } from "lucide-react";
import { BrandLogo } from "@/components/BrandLogo";
import { HospitalNotifications } from "../components/HospitalNotifications";
import { getHospitalSettings, updateHospitalSettings } from "@/lib/api/hospital";

const navigation = [
  { label: "Dashboard", href: "/hospital/dashboard", icon: LayoutDashboard },
  { label: "Patients", href: "/hospital/patients", icon: Users },
  { label: "Appointments", href: "/hospital/appointments", icon: CalendarDays },
];

type SettingsState = { hospitalName: string; email: string; phone: string; address: string; appointmentAlerts: boolean; checkInAlerts: boolean; recordAlerts: boolean };
const defaultSettings: SettingsState = { hospitalName: "City Hospital", email: "admin@cityhospital.com", phone: "+977 1 4567890", address: "Kathmandu, Nepal", appointmentAlerts: true, checkInAlerts: true, recordAlerts: true };

export default function HospitalSettingsPage() {
  const router = useRouter();
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const [settings, setSettings] = useState(defaultSettings);
  const [saved, setSaved] = useState(false);
  const [saveError, setSaveError] = useState<string | null>(null);

  useEffect(() => {
    getHospitalSettings().then((data) => setSettings({ ...defaultSettings, ...data.settings })).catch(() => undefined);
  }, []);
  const update = <K extends keyof SettingsState>(key: K, value: SettingsState[K]) => setSettings((current) => ({ ...current, [key]: value }));
  const saveSettings = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setSaveError(null); try { const result = await updateHospitalSettings(settings); setSettings({ ...defaultSettings, ...result.settings }); setSaved(true); window.setTimeout(() => setSaved(false), 3000); } catch (error: any) { setSaveError(error.response?.data?.message || "Unable to save settings."); } };
  const logOut = () => { localStorage.removeItem("hospitalToken"); router.push("/login"); };
  const sidebar = (mobile = false) => <aside className={`flex h-full w-[272px] flex-col border-r border-slate-100 bg-white px-4 py-5 ${mobile ? "shadow-2xl" : ""}`}><div className="flex items-center justify-between px-2"><BrandLogo size={35} showText subtitle="Hospital Portal" textClassName="text-[18px] font-black tracking-tight text-[#0057d9] leading-none" subtitleClassName="mt-1 text-[10px] font-bold tracking-wide text-slate-400" />{mobile && <button type="button" onClick={() => setMobileMenuOpen(false)} className="rounded-lg p-2 text-slate-400 hover:bg-slate-50" aria-label="Close menu"><X size={18} /></button>}</div><nav className="mt-9 space-y-1" aria-label="Hospital navigation"><p className="px-3 pb-2 text-[10px] font-extrabold uppercase tracking-[0.14em] text-slate-400">Workspace</p>{navigation.map(({ label, href, icon: Icon }) => <Link key={label} href={href} onClick={() => setMobileMenuOpen(false)} className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-[13px] font-bold text-slate-500 transition-colors hover:bg-slate-50 hover:text-slate-900"><Icon size={18} strokeWidth={2.2} />{label}</Link>)}</nav><div className="mt-auto border-t border-slate-100 pt-5"><Link href="/hospital/settings" onClick={() => setMobileMenuOpen(false)} className="flex w-full items-center gap-3 rounded-xl bg-[#0057d9] px-3 py-2.5 text-left text-[13px] font-bold text-white shadow-sm"><Settings size={18} strokeWidth={2.2} />Settings</Link><button type="button" onClick={logOut} className="mt-2 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-[13px] font-bold text-slate-500 hover:bg-rose-50 hover:text-rose-600"><LogOut size={18} strokeWidth={2.2} />Log out</button></div></aside>;

  return <div className="min-h-screen bg-[#f6f8fc] text-slate-900"><div className="fixed inset-y-0 left-0 z-30 hidden lg:block">{sidebar()}</div>{mobileMenuOpen && <div className="fixed inset-0 z-50 lg:hidden"><button type="button" onClick={() => setMobileMenuOpen(false)} className="absolute inset-0 bg-slate-950/35" aria-label="Close menu" /><div className="relative h-full w-[272px]">{sidebar(true)}</div></div>}<div className="lg:pl-[272px]"><header className="sticky top-0 z-20 flex h-[72px] items-center border-b border-slate-100 bg-white/95 px-4 backdrop-blur md:px-7"><button type="button" onClick={() => setMobileMenuOpen(true)} className="mr-3 rounded-lg p-2 text-slate-500 hover:bg-slate-50 lg:hidden" aria-label="Open menu"><Menu size={21} /></button><div className="flex-1"><p className="text-sm font-extrabold text-slate-800">Hospital settings</p></div><div className="flex items-center gap-2.5"><HospitalNotifications /><div className="hidden h-7 w-px bg-slate-200 sm:block" /><Link href="/hospital/profile" className="flex items-center gap-2"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-blue-100 text-xs font-extrabold text-[#0057d9]">CH</div><div className="hidden sm:block"><p className="text-xs font-extrabold text-slate-800">City Hospital</p><p className="mt-0.5 text-[10px] font-medium text-slate-400">Hospital Admin</p></div><ChevronDown size={15} className="hidden text-slate-400 sm:block" /></Link></div></header><main className="mx-auto max-w-5xl p-4 md:p-7"><div className="mb-7"><p className="text-xs font-bold text-[#0057d9]">Hospital workspace</p><h1 className="mt-1 text-2xl font-extrabold tracking-tight md:text-[28px]">Settings</h1><p className="mt-1 text-sm font-medium text-slate-500">Manage your hospital profile and portal notifications.</p></div><form onSubmit={saveSettings} className="space-y-6"><section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm md:p-6"><div className="flex items-start gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-blue-50 text-[#0057d9]"><Building2 size={19} /></div><div><h2 className="text-base font-extrabold">Hospital profile</h2><p className="mt-0.5 text-xs font-medium text-slate-400">Details shown throughout your hospital workspace.</p></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-slate-600 sm:col-span-2">Hospital name<input value={settings.hospitalName} onChange={(event) => update("hospitalName", event.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-medium outline-none focus:border-blue-400" /></label><label className="text-xs font-bold text-slate-600">Admin email<input type="email" value={settings.email} onChange={(event) => update("email", event.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-medium outline-none focus:border-blue-400" /></label><label className="text-xs font-bold text-slate-600">Phone number<input value={settings.phone} onChange={(event) => update("phone", event.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-medium outline-none focus:border-blue-400" /></label><label className="text-xs font-bold text-slate-600 sm:col-span-2">Address<input value={settings.address} onChange={(event) => update("address", event.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-medium outline-none focus:border-blue-400" /></label></div></section><section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm md:p-6"><div className="flex items-start gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-violet-50 text-violet-600"><Bell size={19} /></div><div><h2 className="text-base font-extrabold">Notification preferences</h2><p className="mt-0.5 text-xs font-medium text-slate-400">Choose which operational alerts appear in the portal.</p></div></div><div className="mt-5 divide-y divide-slate-100">{[{ key: "appointmentAlerts", title: "Appointment updates", description: "New, changed, and cancelled appointments." }, { key: "checkInAlerts", title: "Patient check-ins", description: "Updates when patients arrive or are checked in." }, { key: "recordAlerts", title: "Medical record activity", description: "New notes and updates to patient records." }].map(({ key, title, description }) => <label key={key} className="flex cursor-pointer items-center justify-between gap-5 py-4 first:pt-0 last:pb-0"><span><span className="block text-sm font-bold text-slate-700">{title}</span><span className="mt-0.5 block text-xs font-medium text-slate-400">{description}</span></span><input type="checkbox" checked={settings[key as keyof Pick<SettingsState, "appointmentAlerts" | "checkInAlerts" | "recordAlerts">]} onChange={(event) => update(key as "appointmentAlerts" | "checkInAlerts" | "recordAlerts", event.target.checked)} className="h-5 w-5 shrink-0 rounded border-slate-300 text-[#0057d9] focus:ring-blue-200" /></label>)}</div></section><section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm md:p-6"><div className="flex items-start gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-emerald-50 text-emerald-600"><ShieldCheck size={19} /></div><div><h2 className="text-base font-extrabold">Portal access</h2><p className="mt-0.5 text-xs font-medium text-slate-400">Your hospital administrator access is active.</p></div></div><button type="button" onClick={() => router.push("/dashboard/password")} className="mt-5 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-700 hover:bg-slate-50">Change password</button></section><div className="flex items-center justify-end gap-3"><span aria-live="polite" className="text-xs font-bold text-emerald-600">{saved && "Settings saved"}</span>{saveError && <span role="alert" className="text-xs font-bold text-rose-600">{saveError}</span>}<button type="submit" className="inline-flex items-center gap-2 rounded-xl bg-[#0057d9] px-4 py-2.5 text-sm font-bold text-white shadow-sm hover:bg-blue-700"><Save size={16} /> Save changes</button></div></form></main></div></div>;
}
